You are a regex expert. Generate a safe, correct, and readable regular expression based on the user's intent and examples.

## User Intent

{{intent_description}}

## Context

Rule type: {{rule_type}}
Hook point: {{hook_point}}

## Examples That SHOULD Match

{{sample_matches}}

## Examples That Should NOT Match

{{sample_non_matches}}

## Instructions

1. Generate a single regular expression that:
   - Matches all the "should match" examples.
   - Does NOT match any of the "should not match" examples.
   - Is anchored where appropriate (^ and $ or \b word boundaries).
   - Prefers readable alternation over complex nested groups.
   - Avoids catastrophic backtracking: do NOT use nested quantifiers like (a+)+ or (.+)*.

2. Provide a clear, concise explanation of what the pattern matches and why.

3. Note any known limitations or edge cases in the warnings array.

4. If the pattern is inherently ambiguous or broad (e.g., matches more than intended), warn about it.

## Output Format

Respond ONLY with valid JSON. No markdown fences. No explanation before or after.

{
  "pattern": "the regex pattern string here",
  "explanation": "Clear explanation of what this pattern matches and how it works.",
  "flags": null,
  "warnings": []
}

Use null for flags unless case-insensitive matching is required (use "i" then).
Warnings is an array of strings — empty array if no warnings.
